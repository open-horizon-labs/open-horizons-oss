{
  "mcpServers": {
    "rna-mcp": {
      "type": "stdio",
      "command": "repo-native-alignment",
      "args": ["--repo", ".", "--log-path", "/tmp/oh-oss-rna-mcp.log"],
      "env": {
        "RUST_LOG": "repo_native_alignment=info"
      },
      "timeout": 60000
    }
  }
}
