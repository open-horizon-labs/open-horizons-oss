# Database connection (required)
DATABASE_URL="postgres://postgres:postgres@localhost:5432/open_horizons"

# LLM config (optional, server-side only)
# Required for markdown import feature
OPENAI_API_KEY=""
